<ContentDialog
    x:Class="Screenbox.Dialogs.DiscSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dialogs="using:Screenbox.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:strings="using:Screenbox.Strings"
    Title="{strings:Resources Key=DiscSelectionDialogTitle}"
    CloseButtonText="{strings:Resources Key=Close}"
    DefaultButton="Primary"
    PrimaryButtonText="{strings:Resources Key=Open}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ListView
        x:Name="ItemsList"
        MaxHeight="340"
        MinWidth="280"
        SelectionChanged="ItemsList_OnSelectionChanged"
        SelectionMode="Single">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="dialogs:DiscSelectionItem">
                <StackPanel Spacing="2">
                    <TextBlock
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="{x:Bind Name}" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind Subtitle}"
                        TextTrimming="CharacterEllipsis" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentDialog>
